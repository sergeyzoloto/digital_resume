# Task List

## 1. Separate sections into a separate component and keep working with a unified section format

### Steps:

1. Create a `Section` component in `src/components/ui/section.tsx`:
   - Accept props like `id`, `title`, `description`, and `children`.
   - Apply consistent styling for all sections.
2. Refactor existing sections (`HeroSection`, `SkillsSection`, etc.) to use the new `Section` component.
3. Test all sections to ensure they render correctly.

---

## 2. Make a slider in the experience section for mobile screens

### Steps:

1. Import the `Carousel` component from the `shadcn` library.
2. Wrap the `ExperienceSection` content inside the `Carousel` component.
3. Configure the `Carousel` for horizontal scrolling on mobile screens.
4. Add fallback styling for larger screens (e.g., grid layout).
5. Test the slider functionality on mobile devices.

---

## 3. Replace the light/dark mode selection button with a dialogue box call button

### Steps:

1. Replace the `ModeToggle` button in `Header` with a button that opens a `Dialog` component.
2. Create a `ThemeDialog` component:
   - Use the `Dialog` component from `@radix-ui/react-dialog`.
   - Include options for light and dark mode selection.
3. Ensure the `Dialog` opens and closes correctly.

---

## 4. Replace three modes with two, default to system mode to use a toggle switch for light/dark instead

### Steps:

1. Update the `ThemeProvider` logic:
   - Remove the "system" mode.
   - Default to the system theme but allow toggling between light and dark modes.
2. Replace the dropdown menu in `ThemeDialog` with a toggle switch for light/dark mode.
3. Test the toggle functionality.

---

## 5. Replace the page content with content from the dataset, which is stored in a separate variable

### Steps:

1. Create a `content.ts` file in `src/data/` to store the dataset.
2. Replace hardcoded content in sections with dynamic content from the dataset.
3. Use TypeScript types to ensure data consistency.
4. Test all sections to verify the content renders correctly.

---

## 6. Transfer the actual summary to an appendix instead of a sample for the layout

### Steps:

1. Create an `Appendix` section in the dataset for the summary content.
2. Add a new `AppendixSection` component to render the summary.
3. Place the `AppendixSection` at the end of the page layout.
4. Test to ensure the summary is displayed correctly.

---

## 7. Add Russian and Dutch translations to the dataset

### Steps:

1. Extend the dataset in `content.ts` to include translations for all content in Russian and Dutch.
2. Use a nested structure for translations (e.g., `content.en`, `content.ru`, `content.nl`).
3. Test to ensure the dataset structure is correct.

---

## 8. Add a language selection option to the dialogue box

### Steps:

1. Update the `ThemeDialog` component to include a language selection dropdown.
2. Store the selected language in the `ThemeProvider` context.
3. Dynamically render content based on the selected language.
4. Test the language selection functionality.

---

## Combined Testing

1. Test the entire application to ensure all tasks are integrated seamlessly.
2. Verify responsiveness, accessibility, and functionality across browsers.
